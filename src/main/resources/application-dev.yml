spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/dopamine_shop}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

app:
  jwt:
    secret: ${JWT_SECRET:change-this-secret-in-prod-min-32-chars-long}

logging:
  level:
    com.dopamineshop: DEBUG
